-- 로그인이 없어 브라우저가 "현재 사용자"를 아직 고르지 않았을 때 실제 팀원 중 아무나로
-- 오인 귀속(누가 한 것도 아닌데 특정 팀원 이름으로 기록됨)되는 걸 막기 위해 둔 플레이스홀더 멤버.
--
-- 이후 "현재 사용자"를 멤버 목록에서 고르는 방식이 아니라 본인이 직접 입력하는 방식으로 바뀌면서
-- (입력 전에는 아무 기록도 남길 수 없으므로) 앱 코드는 더 이상 이 멤버를 참조하지 않는다.
-- 마이그레이션 이력을 깨지 않기 위해 파일은 남겨두며, 필요 없으면 멤버 관리에서 지워도 된다.
insert into public.users (id, email, name)
values ('11111111-1111-1111-1111-111111111111', 'default-user@example.com', '홍길동')
on conflict (id) do nothing;
